import React, {useEffect} from 'react';
import {
  StyleSheet,
  View,
  Text,
  Image,
  TouchableOpacity,
  TextInput,
} from 'react-native';

//할지말지 보류

//axios
import axios from 'axios';

const GetMyStory = ({TabNavigation, name, user_id}) => {
  useEffect(() => {});

  return (
    <View style={{flex: 1, backgroundColor: 'white'}}>
      <Text>my story</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default GetMyStory;
